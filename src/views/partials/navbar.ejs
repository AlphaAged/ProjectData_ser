<nav class="navbar">
  <div class="nav-left">
    <a href="/" class="<%= currentPath === '/' ? 'active' : '' %>">หน้าหลัก</a>
    <!-- <a href="/search" class="<%= currentPath.startsWith('/search') ? 'active' : '' %>">ค้นหาสรุป</a> -->
    <a href="/community" class="<%= currentPath.startsWith('/community') ? 'active' : '' %>">พูดคุย</a>

    <% if (currentUser) { %>
      <a href="/notifications" class="<%= currentPath.startsWith('/notifications') ? 'active' : '' %>">แจ้งเตือน</a>
      <% if (currentUser.role === 'admin') { %>
        <a href="/admin" class="<%= currentPath.startsWith('/admin') ? 'active' : '' %>">แอดมิน</a>
      <% } %>
    <% } %>
  </div>

  <div class="nav-right">
    <% if (currentUser) { %>
      <form action="/logout" method="post" style="display:inline">
        <button class="linklike">ออกจากระบบ</button>
      </form>
  <a href="/profile" class="<%= currentPath.startsWith('/profile') ? 'active' : '' %>">โปรไฟล์ (<%= currentUser.username %>)</a>
    <% } else { %>
  <a href="/login" class="<%= currentPath.startsWith('/login') ? 'active' : '' %>">เข้าสู่ระบบ</a>
  <a href="/register" class="<%= currentPath.startsWith('/register') ? 'active' : '' %>">สมัครสมาชิก</a>
    <% } %>
  </div>

  <!-- Burger Button (แสดงเมื่อจอเล็ก) -->
  <div class="burger" id="burger">&#9776;</div>
</nav>

<!-- เมนูสำหรับจอเล็ก (slide from right) -->
  <div class="side-menu" id="sideMenu">
  <button class="close-btn" id="closeBtn">&times;</button>
  <a href="/" class="<%= currentPath === '/' ? 'active' : '' %>">หน้าหลัก</a>
  <!-- <a href="/search" class="<%= currentPath.startsWith('/search') ? 'active' : '' %>">ค้นหาสรุป</a> -->
  <a href="/community" class="<%= currentPath.startsWith('/community') ? 'active' : '' %>">พูดคุย</a>

  <% if (currentUser) { %>
  <a href="/notifications" class="<%= currentPath.startsWith('/notifications') ? 'active' : '' %>">แจ้งเตือน</a>
    <% if (currentUser.role === 'admin') { %>
      <a href="/admin">แอดมิน</a>
    <% } %>
    <form action="/logout" method="post" style="display:inline">
      <button class="linklike">ออกจากระบบ</button>
    </form>
  <a href="/profile" class="<%= currentPath.startsWith('/profile') ? 'active' : '' %>">โปรไฟล์ (<%= currentUser.username %>)</a>
  <% } else { %>
    <a href="/login" class="<%= currentPath.startsWith('/login') ? 'active' : '' %>">เข้าสู่ระบบ</a>
    <a href="/register" class="<%= currentPath.startsWith('/register') ? 'active' : '' %>">สมัครสมาชิก</a>
  <% } %>
</div>
