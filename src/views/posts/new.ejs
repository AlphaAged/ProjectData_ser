<!DOCTYPE html>
<html lang="th">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CP KKU Study Notes</title>
  <link rel="stylesheet" href="/style.css" />
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
</head>

<body>
  

  <main class="container">
    <h2 class="d-flex justify-content-center ">สร้างโพสต์สรุป</h2>
    <div class="d-flex justify-content-center align-items-center min-vh-50">
      <div class="preference" style="max-width: 800px; width: 100%;">
        <form method="post" action="/posts" enctype="multipart/form-data">



          <div class="mb-3">
            <label for="objectname" class="form-label fs-5">ชื่อเรื่อง</label>
            <input name="title" class="form-control" id="NoteTitle" placeholder="Note Title" required>
          </div>

          <div class="mb-3">
            <label for="objectdes" class="form-label fs-5">เนื้อหา</label>
            <textarea name="body" rows="8" class="form-control" id="description" placeholder="description"
              required></textarea>
          </div>

          <div class="mb-3">
            <label class="form-label fs-5">แท็ก (เลือกอย่างน้อย 1)</label>
            <select name="tags" class="form-select" required>
              <% ['CS','AI','IT','CY','GIS','Physics','Chemistry','Biology','Math','English','GE','Free'].forEach(t=> {
                %>
                <option value="<%= t %>">
                  <%= t %>
                </option>
                <% }) %>
            </select>
          </div>

          <div class="mb-3">
            <label class="form-label fs-5">ภาพหน้าปก</label>
            <input type="file" name="cover" class="form-control" accept="image/*" onchange="previewImage(event)" />
          </div>

          <!-- Preview -->
          <div class="text-center mt-3">
            <img id="coverPreview" src="#" alt="Preview" class="img-fluid rounded shadow-sm d-none"
              style="max-height: 300px;" />
          </div>

          <script>
            function previewImage(event) {
              const input = event.target;
              const preview = document.getElementById("coverPreview");

              if (input.files && input.files[0]) {
                const reader = new FileReader();
                reader.onload = function (e) {
                  preview.src = e.target.result;
                  preview.classList.remove("d-none"); 
                }
                reader.readAsDataURL(input.files[0]);
              } else {
                preview.src = "#";
                preview.classList.add("d-none"); 
              }
            }
          </script>

          <div class="d-flex justify-content-end mt-3">
            <button type="submit" class="btn btn-primary">โพสต์</button>
          </div>

        </form>
      </div>
    </div>

  </main>

</body>

</html>
