<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>แก้ไขหัวข้อ · CP KKU Study Notes</title>
    <link rel="stylesheet" href="/style.css" />
    <style>
        .wrap {
            max-width: 760px;
            margin: 0 auto;
            padding: 16px
        }

        .form-card {
            background: #fff;
            border: 1px solid #e5e7eb;
            border-radius: 12px;
            padding: 16px
        }

        .action-row {
            display: flex;
            gap: .5rem;
            flex-wrap: wrap;
            margin-top: 12px;
            justify-content: flex-end;
        }

        input[type="text"],
        textarea {
            width: 100%;
            padding: 10px 12px;
            border: 1px solid #d1d5db;
            border-radius: 10px;
            outline: none;
            box-sizing: border-box;
        }

        input[type="text"]:focus,
        textarea:focus {
            border-color: #0d63a5;
            box-shadow: 0 0 0 3px rgba(13, 99, 165, .12);
            box-sizing: border-box;

        }

        .btn {
            padding: 10px 14px;
            border-radius: 10px;
            border: 1px solid #d1d5db;
            background: #f9fafb;
            color: #374151;
            cursor: pointer;
            text-decoration: none
        }

        .btn.primary {
            border-color: #0d63a5;
            background: #0d63a5;
            color: #fff
        }

        .btn.light {
            background: #fff
        }
    </style>
</head>

<body>
    <main class="wrap">
        <div class="form-card">
            <h2>แก้ไขหัวข้อ</h2>
            <form method="post" action="/threads/<%= thread._id %>">
                <div>
                    <label for="title">ชื่อหัวข้อ</label>
                    <input id="title" name="title" type="text" required value="<%= thread.title %>">
                </div>

                <div style="margin-top:10px;">
                    <label for="body">เนื้อหา</label>
                    <textarea id="body" name="body" rows="8" required><%= thread.body %></textarea>
                </div>

                <div class="action-row">
                    <a class="btn light" href="/threads/<%= thread._id %>">ยกเลิก</a>

                    <button class="btn primary" type="submit">บันทึกการแก้ไข</button>
                </div>
            </form>
        </div>
    </main>
</body>

</html>