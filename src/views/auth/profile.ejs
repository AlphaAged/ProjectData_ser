<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>CP KKU Study Notes</title>
  <link rel="stylesheet" href="/style.css"/>
</head>
<body>

<main class="container">
  <h2>โปรไฟล์ของฉัน</h2>

  <% if (message){ %>
    <div class="alert"><%= message %></div>
  <% } %>

  <div class="profile-info">
    <p><strong>ชื่อผู้ใช้:</strong> <%= user.username %></p>
    <p><strong>อีเมล:</strong> <%= user.email %></p>
    <p><strong>สาขา/โปรแกรม:</strong> <%= user.program || '-' %></p>
    <p><strong>ชั้นปี:</strong> <%= user.year || '-' %></p>
    <p><strong>แนะนำตัว:</strong> <%= user.bio || '-' %></p>
  </div>

  <a href="/edit-profile">แก้ไขโปรไฟล์ </a>
  <a href="/saved-posts">รายการที่บันทึก</a>
</main>

</body>
</html>
