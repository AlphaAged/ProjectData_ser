<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>CP KKU Study Notes</title>
  <link rel="stylesheet" href="/style.css"/>
</head>
<body>
  <%- include('../partials/header') %>
<main class="container">
<% if (message){ %><div class="alert"><%= message %></div><% } %>
<form method="post" class="profile-form">
  <h2>แก้ไขโปรไฟล์ของฉัน</h2>

  <div class="form-row">
    <div class="form-group">
      <label>ชื่อผู้ใช้</label>
      <input name="username" value="<%= user.username %>" />
    </div>
    <div class="form-group">
      <label>อีเมล</label>
      <input name="email" value="<%= user.email %>" />
    </div>
  </div>

  <div class="form-row">
    <div class="form-group">
      <label>สาขา</label>
      <select name="program">
        <option value="">-- เลือกสาขา --</option>
        <option value="CS" <%= user.program === 'CS' ? 'selected' : '' %>>CS</option>
        <option value="IT" <%= user.program === 'IT' ? 'selected' : '' %>>IT</option>
        <option value="GIS" <%= user.program === 'GIS' ? 'selected' : '' %>>GIS</option>
        <option value="CY" <%= user.program === 'CY' ? 'selected' : '' %>>CY</option>
        <option value="AI" <%= user.program === 'AI' ? 'selected' : '' %>>AI</option>
      </select>
    </div>
    <div class="form-group">
      <label>ชั้นปี</label>
      <select name="year">
        <option value="">-- เลือกชั้นปี --</option>
        <option value="1" <%= user.year === '1' ? 'selected' : '' %>>1</option>
        <option value="2" <%= user.year === '2' ? 'selected' : '' %>>2</option>
        <option value="3" <%= user.year === '3' ? 'selected' : '' %>>3</option>
        <option value="4" <%= user.year === '4' ? 'selected' : '' %>>4</option>
      </select>
    </div>
  </div>

  <div class="form-group">
    <label>แนะนำตัว</label>
    <textarea name="bio" rows="5"><%= user.bio||'' %></textarea>
  </div>

  <div class="form-actions">
    <a id="cancel-btn" class="linklike" href="/profile">ยกเลิก</a>
    <button id="saved-btn" class="button" type="submit">บันทึก</button>
  </div>
</form>

</main>
</body>
</html>
